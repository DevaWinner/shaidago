# BE-097 live Source Scout evidence

**Status: pending. No live provider call has been made.** The unattended build run was not permitted to call Brave, OpenAI, or any paid service (ADR-0008), so the required controlled live run is left for the maintainer.

## What is already proven without a live call

- Replay fixtures for every scenario the task lists (success, no results, duplicates, contradictions, unsafe URLs, prompt injection, stale page, provider outage, extraction failure, invalid model result, cancellation, and dead letter) are generated by `services/platform/scripts/build_discovery_fixtures.py` from synthetic definitions and exercised end to end by `tests/integration/test_discovery_replay.py`. The fixtures describe a fictional project on reserved `.test` domains; nothing in them is a real fact, page, or person.
- Every replay result is labelled `demo_replay: true` in the run response and in the stored analysis, and a replay is never presented as a live search or a live answer.

## What the maintainer must do before submission

1. Put real `SEARCH_API_KEY` and `OPENAI_API_KEY` values in a local, uncommitted `.env` and set `PROVIDER_MODE=live` (never on a shared or production database with real reports).
2. Run the opt-in search check with a real **public** project title from `docs/SOURCE_REGISTER.md`:
   `SHAIDAGO_LIVE_TESTS=1 SEARCH_API_KEY=... LIVE_PROJECT_TITLE="..." uv run pytest tests/live -m live`
3. Start the worker (`make worker`), start one public run for that project, and review the resulting source cards and analysis by hand.
4. Record below **only** safe evidence:

| Field | Value |
| --- | --- |
| Run date and time (UTC) | _pending_ |
| Query policy version | `query-policy-2026-09-v1` (confirm it is unchanged) |
| Search provider and version | Brave, `brave-web-v1` |
| Analysis model ID and prompt version | _pending_ (`discovery-analysis-v1`) |
| Result counts (found, fetched, analysed) | _pending_ |
| Reviewed public URLs | _pending_ |
| Reviewer and review date | _pending_ |

Do **not** record keys, raw prompts, raw responses, provider snippets, or any private value. Do not treat any live result as a verified fact: it stays `discovered — not yet reviewed` until a reviewer attaches it and it passes the ordinary source review.
