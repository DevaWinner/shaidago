# BE-114 staging deployment, rotation, rollback, and smoke record

**Status: partial.** The staging environment was created and the backend deployed on Railway, a secret rotation and a rollback were exercised, and the container was verified. The full fictional journey has **not** been run on staging, because it needs access inside Railway's private network and no SSH key is registered on the Railway account (registering one is an account-level change left to the maintainer).

## Environment

- Railway project `shaidago-staging`, environment `staging` (a separate project from any production project; nothing shared).
- Deployed from commit `a19cabd` (uploaded from the working tree of the `deployment-operations` branch).
- Services: `postgres` (`pgvector/pgvector:pg18` with a volume), `Redis` (managed), a private bucket (`ams`), and `api`, `worker`, and `migrate` built from `services/platform/Dockerfile` at the commit recorded below. None has a public domain; there is no web service yet.
- Providers: `PROVIDER_MODE=replay` (no live provider key was set or used), `SCANNER_MODE=not_deployed` (the hosted demo mode), all keys and role passwords freshly generated by `scripts/railway_staging_variables.py` and never printed or committed.

## What was exercised

| Step | Result |
| --- | --- |
| Migration job (`migrate`) deployed first, one-shot with the owner URL only | SUCCESS: `alembic upgrade head` then role provisioning ("enabled login for 4 roles" in its log) |
| `api` deployed after `migrate` | SUCCESS: container healthcheck `/health/live` passed; log shows startup complete and a liveness request |
| `worker` deployed from the same image | SUCCESS: log shows "Worker process is ready for action" |
| Secret rotation | `INTERNAL_WEB_CREDENTIAL_CURRENT` replaced and the old value kept as `_PREVIOUS` for the overlap window; the service redeployed |
| Rollback | The `api` deployment `b9ed3739` was rolled back to after the rotation deployment `256a7a7f` succeeded (`deploymentRollback` through the Railway API): the rollback deployment `be30438c` reached SUCCESS and its log shows startup complete. The schema was untouched, as the runbook prescribes. |

## Not exercised (needs the maintainer)

Run once, from a machine with a registered Railway SSH key (`railway ssh keys add`), against `api`:

1. Seed the synthetic staging fixtures with `psql` on the `postgres` service (a fictional project titled "Fixture Scenario Success", one approved synthetic source version, and a staging reviewer whose Argon2id hash is generated locally).
2. Run `scripts/staging_smoke.py` inside the `api` container (instructions in its header). It covers public project read, anonymous fictional submission, tracking, reviewer sign-in, decision, stale-decision refusal, verification, separately authored and previewed publication, Q&A in replay, public Source Scout from replay fixtures, a report-scoped reviewer run and its invisibility to the public, sign-out and revocation, and the internal credential not being reviewer authority. It prints one PASS or FAIL line per step and never a code, token, or report text.
3. Paste its output here.

Limitations: replay providers only; scanner not deployed (files would be labelled unscanned); no CDN or web service in front of the API.
