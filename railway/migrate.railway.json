{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "services/platform/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'alembic upgrade head && python -m shaidago.db.provision'",
    "restartPolicyType": "NEVER"
  }
}
